language: python
python:
  - "3.6"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script:
  - python3 film.py
# blocklist
branches:
  except:
  - master

# safelist
branches:
  only:
  - film_daily
